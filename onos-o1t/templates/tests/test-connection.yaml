apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onos-o1t.fullname" . }}-test-connection"
  labels:
{{ include "onos-o1t.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "onos-o1t.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
